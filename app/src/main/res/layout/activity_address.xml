<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".activity.AddressActivity">

    <TextView
        android:id="@+id/tv_skip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SKIP"
        android:textSize="18sp"
        android:layout_marginEnd="10dp"
        android:layout_marginTop="10dp"
        android:textStyle="bold"
        android:layout_alignParentEnd="true"
        android:textColor="@color/black"
        android:drawableEnd="@drawable/ic_baseline_keyboard_double_arrow_right_24" />
    <ImageView
        android:id="@+id/image_dummy1"
        android:layout_above="@id/tv_addressM"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/ic_baseline_add_24"
        android:scaleType="centerCrop"
        android:layout_margin="10dp"
        android:padding="10dp"
        android:contentDescription="image" />

    <TextView
        android:id="@+id/tv_addressM"
        android:layout_above="@+id/tv_addressC"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:padding="10dp"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="@color/red"
        android:text="Enter Manually"/>

    <TextView
        android:id="@+id/tv_addressC"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/colorFontBlue"
        android:textSize="20sp"
        android:padding="10dp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="50dp"
        android:text="Current Address"/>

</RelativeLayout>